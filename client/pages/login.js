import React from "react";

const Login = () => {
  return (
    <div>
      <h1>Login | Code-NEXT</h1>
      <input type="email" placeholder="email" />
      <input type="password" placeholder="password" />
      <button>Login</button>
    </div>
  );
};

export default Login;
